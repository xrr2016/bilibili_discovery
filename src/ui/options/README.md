# `src/ui/options`

本目录实现扩展设置页。

主要职责：
- 展示和编辑用户配置
- 读取开发环境辅助配置
- 提供跳转到统计页等入口

设计边界：
- 这里只负责设置页面的表单与交互
- 配置持久化通过数据库实现层完成

主要文件：
- `options.ts`：页面初始化与事件绑定
- `settings.ts`：设置读写与归一化
- `dev-env.ts`：开发环境辅助读取
- `options.html` / `options.css`：页面结构与样式
